[general]
type = adam

comment_str = "The hyperparameter is set in other command line arguments, like the old fashion. This conf is only used to provide another way to invoke Adam optimizer while assuring backward compatibility at the same time."
